name:                D8
version:             0.1.0.0
github:              smunix/D8
license:             BSD3
author:              Providence Salumu
maintainer:          Providence.Salumu@smunix.com
copyright:           BSD8

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on Github at <https://github.com/smunix/D8#readme>

dependencies:
- base >= 4.11
- rio >= 0.1.12.0
- array
- utils
- monad-extras
- text
- bytestring
- megaparsec
- parser-combinators
- strings
- data-default
- unordered-containers
- algebraic-graphs
- hashable
- directory

ghc-options:
- -Wall
- -Wcompat
- -Widentities
- -Wincomplete-record-updates
- -Wincomplete-uni-patterns
- -Wpartial-fields
- -Wredundant-constraints

library:
  source-dirs: src

executables:
  D8-exe:
    main:                Main.hs
    source-dirs:         app
    dependencies:
    - D8
    - optparse-simple

    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N

tests:
  D8-test:
    main:                Spec.hs
    source-dirs:         test
    dependencies:
    - D8
    - hspec

    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
